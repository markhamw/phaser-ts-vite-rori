enum Direction {
    UP,
    DOWN,
    LEFT,
    RIGHT,
    RIGHTANDUP,
    RIGHTANDDOWN,
    LEFTANDUP,
    LEFTANDDOWN,
    IDLE,
}

enum EnemyDirection {
    UP,
    DOWN,
    LEFT,
    RIGHT,
    IDLE,
}



//put this in obj data instead
/* export type Placement = {
    collection: string;
    tag: string;
    location: Location;
    name: string;
    animationname?: string;
    transform?: {
      scale?: number;
    };
    identlines?: string[];
  }; */
